[project]
name = "metaml-taxonomy-knowledge-graph"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.111.0",
    "pydantic>=2.0",
    "networkx>=3.3",
    "numpy>=1.24",
    "fastembed>=0.4.0",
]

[tool.vercel]
entrypoint = "api/app.py"
